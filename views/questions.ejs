<% questions.forEach(function(question) { %>
    <div class="question">
        <h2><%= question.question %></h2>
        <img src="/assets/img/<%= question.image %>" alt="">        
    </div>
<% }); %>
